# P0 build log — first CPU build bring-up

Date: 2026-07-18
Branch: gpu-proving-backend
Task: task-1 (first-ever build of zkip-stark against `ix`)

## Environment

- `lake --version`: Lake version 5.0.0-src+797c613 (Lean version 4.24.0)
- `lean --version`: Lean (version 4.24.0, x86_64-unknown-linux-gnu, commit 797c613eb9)
- `cargo --version`: cargo 1.96.1 (356927216 2026-06-26)
- `lean-toolchain` (pinned, before any command): `leanprover/lean4:v4.24.0`

## Step 1: `lake update`

Command: `lake update`

Ran to completion, but with an unexpected side effect: Lake detected that
`ix`'s own `lean-toolchain` requires `leanprover/lean4:v4.29.0`, and
**auto-rewrote this repo's `lean-toolchain` file to `v4.29.0`**, then
restarted itself via elan on the new toolchain (downloading
`lean-4.29.0-linux.tar.zst` in the process):

```
info: zk_ip_protocol: no previous manifest, creating one from scratch
info: ix: cloning https://github.com/argumentcomputer/ix.git
info: ix: checking out revision 'a75cb041c88f044571a9b98a81e60be63acbfbb3'
info: updating toolchain to 'leanprover/lean4:v4.29.0'
info: restarting Lake via Elan
info: downloading https://releases.lean-lang.org/lean4/v4.29.0/lean-4.29.0-linux.tar.zst
info: installing /home/mlayug/.elan/toolchains/leanprover--lean4---v4.29.0
info: zk_ip_protocol: no previous manifest, creating one from scratch
info: toolchain not updated; already up-to-date
info: batteries: cloning ... rev '756e3321fd3b02a85ffda19fef789916223e578c'
info: Cli: cloning ... rev '7802da01beb530bf051ab657443f9cd9bc3e1a29'
info: Blake3: cloning ... rev 'd15f36cf76eb5834b0e623e02b97fd4d95e56cc7'
info: LSpec: cloning ... rev 'd3c15b93a1dd4e7c8d5c0c3825c9555737e55c3e'
```

Resolved `ix` rev (from `lake-manifest.json`, `inputRev: "main"`):

```
a75cb041c88f044571a9b98a81e60be63acbfbb3
```

Confirmed directly: `.lake/packages/ix/lean-toolchain` contains
`leanprover/lean4:v4.29.0`. This is not a lockfile quirk — it is `ix`'s
actual pinned toolchain at `main`.

## Root cause: hard toolchain incompatibility

`ix@main` requires Lean `v4.29.0`. This repo is pinned to Lean `v4.24.0`
per the global project constraint (`lean-toolchain` must stay
`leanprover/lean4:v4.24.0`). These two requirements cannot both be
satisfied simultaneously — Lake enforces a single toolchain for the whole
dependency graph, and it silently adopted `ix`'s requirement over the
repo's own pin.

This is a genuine major-version toolchain gap (4.24 -> 4.29 is 5 stepping
Lean releases), not an import-path drift, a missing system lib, or a
Lean module-system (`module`/`public section`) mismatch — none of the
three triage culprits listed in the task brief apply here. Per the task
brief's stop condition, this is fundamentally incompatible, not a small
fix, so the build was **not attempted** on the auto-bumped 4.29.0
toolchain.

### For context (not acted on): a compatible `ix` rev exists

`ix`'s own history shows a commit that pinned exactly Lean `v4.24.0`,
immediately before it moved on:

```
aa862a7 chore: Update Lean to v4.24.0 and Rust to v1.91 (#255)   <- matches our pin
570b50c chore: Update to Lean v4.26.0 and Rust 1.92 (#290)
a555191 chore: Update Lean to v4.27.0  (#310)
266a6d8 chore: Update Lean to v4.28.0  (#312)
78038b9 chore: Update Lean to v4.29.0 (#385)   <- current main
```

Per the task's global constraint ("Do NOT unpin or change the `ix`
dependency to force a build"), this repo's `require ix ... @ "main"` was
left untouched and `aa862a7` was **not** pinned. Recorded here only as a
lead for whoever resolves the block (a project-level decision, since it
means either following `ix` to 4.29.0 or vendoring/pinning an older `ix`
rev going forward).

## Recovery action taken

`git checkout -- lean-toolchain` to restore the pinned `v4.24.0` value
that Lake had overwritten. Working tree confirmed clean again
(`lean-toolchain` back to `leanprover/lean4:v4.24.0`).

`.lake/packages/` and `lake-manifest.json` were left on disk (both are
gitignored) as evidence/for reuse if a follow-up task changes the
toolchain decision; neither is tracked by git.

## Steps 2-4 (build, triage, run test exe)

**Not attempted.** Step 2 (`lake build`) requires the toolchain that
Step 1 revealed is incompatible with the pinned `lean-toolchain`. Running
it would require either bumping `lean-toolchain` to 4.29.0 or repinning
`ix`, both explicitly disallowed by the task's global constraints.

## Outcome

**BLOCKED.** `ix@main` (rev `a75cb041c88f044571a9b98a81e60be63acbfbb3`)
requires Lean `v4.29.0`; this repo is pinned to Lean `v4.24.0` and the
task disallows changing either pin to force a build. This needs a
project-level decision (follow `ix` to 4.29.0, or pin `ix` to a
4.24.0-compatible rev such as `aa862a7`) before the build bring-up can
proceed.
